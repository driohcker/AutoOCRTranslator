# 基础
PyYAML>=6.0.2
Pillow>=10.0.0
numpy>=1.26.0

# 窗口捕获与系统交互
mss>=9.0.0
pywin32>=306

# GUI 与覆盖层
PyQt6>=6.6.0

# OCR
# 开发环境可选安装 PaddleOCR（体积大）：
#   pip install paddlepaddle>=3.0.0 paddleocr>=3.0.0
# 基础包默认仅打包 RapidOCR，因此生产包体积小。
rapidocr-onnxruntime>=1.2.0
onnxruntime==1.20.1  # 与 onnxruntime-gpu 1.20.1 保持版本一致，便于 GPU 补丁升级

# 翻译 API 支持
requests>=2.31.0

# 测试
pytest>=8.0.0

# 打包
# pyinstaller>=6.0.0
